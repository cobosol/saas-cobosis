{% if user.is_authenticated %}
                    <!-- ===== USUARIO AUTENTICADO ===== -->
                    <div class="user-dropdown">
                        <!-- Botón que muestra el dropdown -->
                        <button class="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-gray-50 transition-colors">
                            <div class="user-avatar relative">
                                {{ user.first_name|first|upper }}{{ user.last_name|first|upper }}
                            </div>
                            <div class="hidden md:block text-left">
                                <span class="block text-sm font-semibold text-gray-900 leading-tight">
                                    {{ user.get_full_name|default:user.username }}
                                </span>
                                <span class="block text-xs text-gray-500 leading-tight">Mi cuenta</span>
                            </div>
                            <i class="bi bi-chevron-down text-xs text-gray-400 ml-1"></i>
                        </button>
                        
                        <!-- Menú desplegable -->
                        <div class="user-dropdown-menu">
                            <!-- Información del usuario -->
                            <div class="user-info">
                                <span class="user-name">{{ user.get_full_name|default:user.username }}</span>
                                <span class="user-email">{{ user.email }}</span>
                            </div>
                            
                            <!-- Enlaces -->
                            <a href="{% url 'profile' %}">
                                <i class="bi bi-person-gear"></i> Mi Perfil
                            </a>
                            
                            <a href="#">
                                <i class="bi bi-credit-card"></i> Mis Suscripciones
                            </a>
                            
                            <a href="#">
                                <i class="bi bi-megaphone"></i> Mis Promociones
                            </a>
                            
                            <a href="#">
                                <i class="bi bi-cart-check"></i> Mis Pedidos
                            </a>
                            
                            <div class="dropdown-divider"></div>
                            
                            <!-- Cerrar sesión -->
                            <form method="post" action="{% url 'logout' %}" class="m-0">
                                {% csrf_token %}
                                <button type="submit">
                                    <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                                </button>
                            </form>
                        </div>
                    </div>
                    {% else %}
                    <!-- ===== USUARIO NO AUTENTICADO ===== -->
                    <a href="{% url 'login' %}?next={{ request.path }}" 
                       class="text-gray-600 hover:text-[#6C63FF] px-4 py-2 text-sm font-medium transition-colors whitespace-nowrap">
                        Iniciar Sesión
                    </a>
                    <a href="{% url 'registro' %}" 
                       class="bg-[#6C63FF] text-white px-5 py-2.5 rounded-xl text-sm font-semibold hover:bg-[#5A52D5] transition-all duration-300 hover:shadow-lg hover:shadow-[#6C63FF]/25 hover:-translate-y-0.5 whitespace-nowrap">
                        <i class="bi bi-person-plus me-1"></i> Registrarse
                    </a>
                    {% endif %}